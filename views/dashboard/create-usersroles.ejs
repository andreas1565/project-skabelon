<% include partials/head.ejs %>
    <div class="flex">
            <% include partials/menu.ejs %> 
                <section>
                        <form  method="post" class="administrationform">
                                <fieldset>
                                        <label for="name">skriv en name på den nye brugerrolle</label>
                                        <input type="text" name="name" value="<%= (typeof name != 'undefined')? name: '' %>">
                                </fieldset>
                                <fieldset>
                                    <label for="level">level</label>
                                    <input type="number" name="level" value="<%= (typeof level != 'undefined')? level: '' %>">
                                </fieldset>
                            <button type="submit">oprate et products</button>
                            </form>
                            <p>
                                <%= (typeof  errorMessage != "undefined") ?  errorMessage : '' %>
                            </p>  
                </section>    
    </div>
    <% include partials/footer.ejs %>