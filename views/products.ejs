<% include partials/header.ejs %>
    <h1><%= title %></h1>
    <a href="/createproducts">oprate et product</a>
    <table>
            <thead>
                <tr>
                    <th></th>
                    <th>navn</th>
                    <th>description</th>
                    <th>price</th>
                    <th>weight</th>
                    <th>amount</th>
                    <th>categories</th>
                </tr>
            </thead>
            <tbody>
                    <% products.forEach(product => { %>
                <tr>
                    <td>
                        <a href="/editproduct/<%= product.id %>">rediger</a>
                        <a href="/deleteproducts/<%= product.id %>">selt</a>
                    </td>
                    <td> 
                        <%= product.name %>
                    </td>
                    <td>
                        <%=  product.description %>
                    </td>
                    <td>
                        <%= product.price %>
                    </td>
                    <td>
                        <%= product.weight %>
                    </td>
                    <td>
                        <%= product.amount %>
                    </td>
                    <td>
                        <%= product.categoriesname %>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <% include partials/footer.ejs %>